\documentclass[preview]{standalone}
% pdflatex playground.tex
% convert -density 300 playground.pdf -quality 90 file.png
\usepackage{amsmath}
\begin{document}
A key is from a set of attributes $K$ from entity set $E$

such that given two entites $e_1$ and $e_2 \in E,\ e_1 \not = e_2$

% $$\text{A \textit{key} is from a set of attributes } K \text{from entity set E such that given two entities } e_1 \text{ and } e_2 \in E. e_1 \not = e_2$$
\end{document}